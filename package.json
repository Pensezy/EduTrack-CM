{
  "name": "edutrack-monorepo",
  "version": "2.0.0",
  "private": true,
  "description": "EduTrack CM - Système de gestion scolaire modulaire",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/**\" dev",
    "dev:hub": "pnpm --filter hub dev",
    "dev:admin": "pnpm --filter admin dev",
    "dev:academic": "pnpm --filter academic dev",
    "dev:finance": "pnpm --filter finance dev",
    "build": "pnpm --filter \"./apps/**\" build",
    "build:hub": "pnpm --filter hub build",
    "build:admin": "pnpm --filter admin build",
    "build:academic": "pnpm --filter academic build",
    "build:finance": "pnpm --filter finance build",
    "test": "pnpm --filter \"./packages/**\" test",
    "lint": "pnpm --parallel --filter \"./apps/**\" lint",
    "clean": "pnpm --parallel --filter \"./apps/**\" clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "prettier": "^3.1.1",
    "eslint": "^8.56.0"
  },
  "keywords": ["edutrack", "education", "school-management", "cameroon", "monorepo"],
  "author": "EduTrack Team"
}
